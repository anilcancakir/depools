---
paths:
  - "backend/**"
---

<!-- Generated by ac:claude-md-rules-creator on 2026-08-11. The stock write paths have their own rule (ledger.md) because getting them wrong breaks the product's whole promise. -->

# The Laravel API

Applies to `backend/`. Laravel 13, PHP 8.2+, PostgreSQL with pgvector and pg_trgm, UUIDv7 keys.

## Two house rules that reverse a Laravel default

- **No `declare(strict_types=1)`.** Anılcan's call, applied across every file that carried it, so the codebase is uniform rather than half strict. Nothing load-bearing is lost: a non-numeric string still cannot coerce to `int`, so a type error still raises a `TypeError`. What changes is that a numeric string coerces at a scalar parameter, so a quantity arriving as `"6"` is accepted as `6.0` instead of rejected. Validate at the boundary; do not rely on the signature to reject it.
- **No database functions, stored procedures or generated columns** (D84). PostgreSQL stores, indexes and constrains; Laravel computes. A CHECK constraint and a partial index are fine, because they constrain rather than derive. A derived value is either written by PHP with a guard and a drift test, or not stored at all and computed at the boundary.

## Tenancy fails closed, so a derivation has to state its crossing

`TeamScope` resolves `team_id` from the authenticated user, and with no auth context it matches NOTHING rather than everything. That is the right default and it has a consequence worth internalising: a queued job, a scheduled command or any derivation running outside a request silently does nothing under the scope. It does not error, it returns zero rows.

So a derivation outside a request states its crossing explicitly with `withoutGlobalScope(TeamScope::class)`, keyed on the product or lot it was given rather than on a team it looked up (D111). `grep withoutGlobalScope` is the audit for every place this happens.

`team_id` is never fillable. Two consequences: `updateOrCreate` silently drops it and inserts a null, so use `firstOrNew` plus an explicit `setAttribute('team_id', ...)` and then `fill()->save()`; and a raw `attach()` on a pivot inserts a null too, so go through the model method that sets it (`Product::syncTags()` is the worked example).

No route accepts a team identifier in any form. The surest way to keep `team_id` out of a request is for the parameter not to exist.

## Schema

- UUIDv7 primary keys on every table, as a native `uuid` column. Never `$table->id()`.
- Partial unique indexes and `UNIQUE NULLS NOT DISTINCT` are both in use; reach for them before adding an application-level uniqueness check.
- **No environment holds data worth preserving yet, so a schema change edits its migration in place** rather than adding a follow-up one. A local development database does not count as deployed; it is reset with `php artisan migrate:fresh --seed`. The rule inverts the day a shared environment holds real rows.
- A local database therefore goes stale silently: a migration added on another branch never runs there, and the failure surfaces later as `SQLSTATE[42P01] relation "x" does not exist` from something unrelated. `php artisan migrate` after a pull, and `php artisan db:seed` for the demo tenant.
- Every vocabulary that is genuinely closed gets a CHECK constraint. A column whose vocabulary is deliberately still open carries a comment saying so, so the gap reads as a decision rather than an oversight.

## Tests

- **Tests run on PostgreSQL, never SQLite** (D72). The schema uses native `uuid`, vector columns and partial unique indexes, so a suite on SQLite would certify a schema it never built.
- **A failed statement aborts the whole transaction.** Under `RefreshDatabase` every test runs in one transaction, and PostgreSQL puts it in an aborted state after a constraint violation, so every later query in that test fails with `SQLSTATE[25P02]` no matter how unrelated. Wrap each expected refusal in its own `DB::transaction()`, which gives it a savepoint that can roll back alone:

```php
$this->expectException(QueryException::class);

DB::transaction(function (): void {
    // the insert the database is supposed to refuse
});
```

- A name is a sentence about the behaviour being pinned, in snake_case after the `test_` prefix: `test_the_sheet_count_rounds_up_and_zero_labels_need_no_paper`, not `test_sheet_count`. It should say what would be lost if the assertion were deleted, which is also what tells a reader whether a failure matters.
- PHPUnit 12 no longer reads `@dataProvider` doc-comments. Use the `#[DataProvider]` attribute; the doc-comment form fails as "Too few arguments" and points nowhere near the cause.
- A tenancy isolation test is written before the feature it protects, and it asserts 404 rather than 403.
- `pint --test` clean is part of the gate, and `bin/check backend` runs the whole backend half.

## Controllers

Thin: inject the service or the contract, return an API Resource. Paths under `api/v1` are English and plural.

**The app's own URLs are English too, and this rule used to say the opposite.** It read: "the app's own URLs are Turkish, because a screen's URL is read by the person using the product and an API path is read by whoever maintains the client." The premise was right and the conclusion did not follow: a screen's URL IS read by the person using the product, which is exactly why it cannot be in a language they do not read. `AGENTS.md` says the primary market is outside Turkey and the default locale is English, so the two files contradicted each other from the day this one was written. `/products` and `api/v1/products` now agree, and `test/routes/route_paths_test.dart` keeps every screen path lower-case ASCII.
