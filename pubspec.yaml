name: depools
description: "AI-assisted inventory management for small businesses and households."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Hosted (published pub.dev) versions of the magic ecosystem, so a fork
  # copied outside this workspace resolves without any sibling checkout.
  # In-workspace development overrides these to local path checkouts via the
  # gitignored pubspec_overrides.yaml; that file never ships in the fork.
  magic: ^0.0.6
  magic_deeplink: ^0.0.2
  magic_notifications: ^0.0.2
  magic_social_auth: ^0.0.2
  magic_starter: ^0.0.1-alpha.17

  # Dev-tooling, imported by lib/main.dart under kDebugMode (release tree-shakes).
  magic_devtools: ^0.0.2
  fluttersdk_dusk: ^0.0.9
  fluttersdk_telescope: ^0.0.4

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  fluttersdk_artisan: ^0.0.9

  # Pin as a real dependency, not a workspace-only override: magic's own
  # file_picker constraint (">=11.0.2 <12.0.0-0") already excludes the
  # 12.0.0-beta prerelease, but a fork resolving purely from pub.dev has no
  # dependency_overrides safety net, so pin it here too. The beta's non-null
  # saveFile params break magic.
  file_picker: ^11.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  assets:
    # Every locale listed in `localization.supported_locales` must appear here or it is not
    # bundled. `tr.json` loaded fine in debug on web without this line, because the dev server
    # serves the project directory rather than the asset manifest, so the omission would have
    # surfaced first in a release build as a Turkish app rendering raw keys.
    - assets/lang/en.json
    - assets/lang/tr.json
    # Runtime env. Must be bundled so flutter_dotenv can load it on web;
    # without this the app silently falls back to Env defaults
    # (BROADCAST_CONNECTION=null) and realtime never connects. `.env` is
    # COMMITTED rather than gitignored precisely because it is an asset: a
    # missing asset fails `flutter build`, so a gitignored one would leave every
    # fresh clone of this template unbuildable. It carries public client config
    # only; a client bundle cannot hold a secret.
    - .env
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Self-hosted rather than fetched at runtime: the app must render correctly
  # offline and on first launch, and Turkish text must never fall back. Both
  # files are the canonical variable fonts from google/fonts, and both were
  # checked to carry all twelve Turkish glyphs (Ğ ğ İ ı Ş ş Ç ç Ö ö Ü ü).
  #
  # Wind resolves these through `fontFamilies` in lib/main.dart: `font-sans`
  # maps to Inter and is the default for every text style, `font-mono` maps to
  # Geist Mono and is used for quantities, prices and barcodes so digit columns
  # align. See the typography section of DESIGN.md.
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Variable.ttf
    - family: Geist Mono
      fonts:
        - asset: assets/fonts/GeistMono-Variable.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
